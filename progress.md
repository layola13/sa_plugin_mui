# sa_plugin_mui Progress

Overall progress: 100%

## Completed Feature Checkpoints

- 1%: Initial `mui` plugin scaffold created on top of `sa_plugin_react`. The plugin has its own Zig descriptor build, manifest, and a locked `@mui/material` core component manifest derived from `/home/vscode/projects/material-ui/packages/mui-material/src/index.js`. Runtime MUI implementation remains incomplete; current work establishes the source-faithful component inventory and the two-plugin build direction.
- 2%: Added the first SA/SAX component source file at `mui/material.sax`, plus `demos/mui_basic_inlined.sax` and `demos/mui_all_components.sax`. The current implemented component seed covers ButtonBase, Button, IconButton, Fab, Box, Paper, and Typography.
- 3%: Re-aligned the plugin boundary after design review: Zig exports only the descriptor and installs share assets. Removed the command/expansion direction from docs and manifest permissions. MUI behavior, ownerState, slots, classes, theme defaults, and all future component logic must be implemented in SA/SAX and compiled by the React plugin pipeline.
- 4%: Extended the SA/SAX component seed with Container, Stack, and Divider default root-slot implementations, and added explicit owner/default-prop state slots for Paper and Typography. Verified `demos/mui_basic_inlined.sax` with `sa react check` and `sa react build` through `sa_plugin_react`; generated `app.sa` contains the new component exports and MUI utility class constants.
- 5%: Added the Card component family seed in SA/SAX: Card, CardActions, CardContent, and CardMedia. Defaults follow upstream MUI's React model where current SAX can express it: Card is Paper-like root output, CardActions includes default spacing owner state, CardContent is a div root slot, and CardMedia carries image/src state slots. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_card*` exports and `MuiCard*` utility class constants.
- 6%: Added AppBar, Toolbar, Avatar, AvatarGroup, List, ListItem, ListItemIcon, ListItemText, and ListSubheader SA/SAX seeds. Defaults follow upstream root utility class composition where current SAX can express it, with state slots for owner/default props such as color, position, variant, dense, gutters, spacing, primary, and secondary. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains the new `sax_*` exports and `MuiAppBar`/`MuiToolbar`/`MuiAvatar`/`MuiList*` class constants.
- 7%: Added Alert, AlertTitle, Badge, ButtonGroup, Breadcrumbs, and Chip SA/SAX seeds. Defaults follow upstream root/slot utility class composition where current SAX can express it, with state slots for owner/default props such as severity, variant, badgeContent, max, separator, label, size, color, and orientation. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains the new `sax_*` exports, prop setters, and `MuiAlert`/`MuiBadge`/`MuiButtonGroup`/`MuiBreadcrumbs`/`MuiChip` class constants.
- 8%: Added Accordion, AccordionSummary, AccordionDetails, AccordionActions, Backdrop, CircularProgress, LinearProgress, and Skeleton SA/SAX seeds. Defaults follow upstream root/slot utility class composition where current SAX can express it, with state slots for owner/default props such as expanded, disabled, gutters, invisible, open, color, value, thickness, animation, size, and variant. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains the new `sax_*` exports, prop setters, SVG/span slot output, and `MuiAccordion`/`MuiBackdrop`/`MuiCircularProgress`/`MuiLinearProgress`/`MuiSkeleton` class constants.
- 9%: Added the Dialog component family seeds in SA/SAX: Dialog, DialogActions, DialogContent, DialogContentText, and DialogTitle. Defaults follow upstream MUI's React model where current SAX can express it: Dialog emits root/backdrop/container/paper slots with the default paper scroll and `maxWidth="sm"` class set, DialogActions includes default spacing owner state, DialogContent carries `dividers`, and title/content text wrap Typography utility classes. Verified `demos/mui_basic_inlined.sax` with `sa react check` and `sa react build`; generated `app.sa` contains `sax_dialog*` exports, prop setters, and `MuiDialog*` utility class constants. Boundary scan confirms Zig remains descriptor-only with `.handle_command = null` and no MUI logic files under `src/`.
- 10%: Added BottomNavigation, BottomNavigationAction, CardActionArea, and CardHeader SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: BottomNavigation emits the default root slot, BottomNavigationAction emits ButtonBase-like root and label slots with the default `iconOnly` class state, CardActionArea emits ButtonBase-like root plus focusHighlight slot, and CardHeader emits root/content/title Typography slots with owner/default-prop state. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_bottomnavigation*`, `sax_cardactionarea`, and `sax_cardheader` exports, prop setters, and `MuiBottomNavigation*`/`MuiCardActionArea`/`MuiCardHeader` utility class constants. Boundary scan confirms Zig remains descriptor-only with `.handle_command = null` and no MUI logic files under `src/`.
- 11%: Added FormControl, FormGroup, FormHelperText, and FormLabel SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: FormControl emits the default root slot and owner/default-prop state for color, margin, size, variant, required, hiddenLabel, focus, disabled, error, and fullWidth; FormGroup emits the root slot with row/error state; FormHelperText emits the default contained medium helper text class set; and FormLabel emits the default primary label class set. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_formcontrol`, `sax_formgroup`, `sax_formhelpertext`, and `sax_formlabel` exports, prop setters, and `MuiFormControl`/`MuiFormGroup`/`MuiFormHelperText`/`MuiFormLabel` utility class constants. Boundary scan confirms Zig remains descriptor-only with `.handle_command = null` and no MUI logic files under `src/`.
- 12%: Added InputBase, Input, InputAdornment, and InputLabel SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: InputBase emits root/input slots with default `type="text"` state, Input emits the standard underline root/input class set on top of InputBase classes, InputAdornment emits the default start/standard root class set with position/variant state, and InputLabel emits the FormLabel/InputLabel standard animated class set. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_inputbase`, `sax_input`, `sax_inputadornment`, and `sax_inputlabel` exports, prop setters, input/adornment/label slot output, and `MuiInputBase`/`MuiInput`/`MuiInputAdornment`/`MuiInputLabel` utility class constants. Boundary scan confirms Zig remains descriptor-only with `.handle_command = null` and no MUI logic files under `src/`.
- 13%: Added FilledInput, OutlinedInput, TextField, and TextareaAutosize SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: FilledInput emits InputBase-compatible root/input slots with the filled underline class set, OutlinedInput emits root/input plus notchedOutline fieldset/legend structure, TextField emits the FormControl root with outlined InputLabel/Input slots for the default variant, and TextareaAutosize emits the textarea root with minRows/maxRows/value state. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_filledinput`, `sax_outlinedinput`, `sax_textfield`, and `sax_textareaautosize` exports, prop setters, outline/input/textarea slot output, and `MuiFilledInput`/`MuiOutlinedInput`/`MuiTextField`/`MuiTextareaAutosize` utility class constants. Boundary scan confirms Zig remains descriptor-only with `.handle_command = null` and no MUI logic files under `src/`.
- 14%: Added Checkbox, Radio, RadioGroup, Switch, and FormControlLabel SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: Checkbox and Radio emit SwitchBase-like root/input/icon slots with default primary medium classes, RadioGroup emits the FormGroup-compatible radiogroup root, Switch emits root/switchBase/input/thumb/track slots, and FormControlLabel emits a label root with control slot plus Typography label slot. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_checkbox`, `sax_radio`, `sax_radiogroup`, `sax_switch`, and `sax_formcontrollabel` exports, prop setters, input/label/track slot output, and `MuiCheckbox`/`MuiRadio`/`MuiRadioGroup`/`MuiSwitch`/`MuiFormControlLabel` utility class constants. Boundary scan confirms Zig remains descriptor-only with `.handle_command = null` and no MUI logic files under `src/`.
- 15%: Added Select, NativeSelect, MenuItem, and MenuList SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: Select emits an outlined InputBase-compatible root with select/nativeInput/icon/notchedOutline slots, NativeSelect emits the standard native select/input icon structure, MenuItem emits ButtonBase-like list item root with gutters, and MenuList emits the upstream List-based menu container with role menu. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_select`, `sax_nativeselect`, `sax_menuitem`, and `sax_menulist` exports, prop setters, select/menu/list slot output, and `MuiSelect`/`MuiNativeSelect`/`MuiMenuItem`/`MuiList` utility class constants. Boundary scan confirms Zig remains descriptor-only with `.handle_command = null` and no MUI logic files under `src/`.
- 16%: Added Menu, Popover, Popper, Portal, and Modal SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: Modal emits root/backdrop/content slots, Portal emits a pass-through portal root, Popover emits Modal/Paper-compatible root and paper slots, Popper emits a root slot with open/placement state, and Menu emits the Popover/Paper/MenuList slot hierarchy with Menu utility classes. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_menu`, `sax_popover`, `sax_popper`, `sax_portal`, and `sax_modal` exports, prop setters, overlay/menu slot output, and `MuiMenu`/`MuiPopover`/`MuiPopper`/`MuiPortal`/`MuiModal` utility class constants. Boundary scan confirms Zig remains descriptor-only with `.handle_command = null` and no MUI logic files under `src/`.
- 17%: Added Drawer, SwipeableDrawer, Slide, Fade, Grow, and Zoom SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: Drawer emits the temporary Modal/backdrop/Paper drawer hierarchy with default left anchor and elevation 16, SwipeableDrawer mirrors Drawer structure while exposing swipe owner props, and Slide/Fade/Grow/Zoom emit transition pass-through roots with their default appear/in/reduced-motion state slots. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_drawer`, `sax_swipeabledrawer`, `sax_slide`, `sax_fade`, `sax_grow`, and `sax_zoom` exports, prop setters, surface/transition slot output, and `MuiDrawer`/`MuiSlide`/`MuiFade`/`MuiGrow`/`MuiZoom` utility class constants. Boundary scan confirms Zig remains descriptor-only with `.handle_command = null` and no MUI logic files under `src/`.
- 18%: Added Stepper, Step, StepButton, StepConnector, StepContent, StepIcon, and StepLabel SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: Stepper emits the horizontal root with active/non-linear/alternative-label state, Step carries active/completed/disabled/index/last owner state, StepButton wraps a StepLabel-like label slot on a ButtonBase root, StepConnector emits root/line slots, StepContent emits root/transition slots, and StepIcon/StepLabel emit the default icon/label slot hierarchy. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains the new `sax_step*` exports, prop setters, SVG/label/connector slot output, and `MuiStepper`/`MuiStep*` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 19%: Added Tabs, Tab, and TabScrollButton SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: Tabs emits the root/scroller/list/indicator slot hierarchy with default standard horizontal fixed state, Tab emits a ButtonBase-like tab root with selected/disabled/textColor/label/icon owner state, and TabScrollButton emits the ButtonBase-derived root plus default SVG arrow icon slot. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_tabs`, `sax_tab`, and `sax_tabscrollbutton` exports, prop setters, tablist/tab/icon output, and `MuiTabs`/`MuiTab`/`MuiTabScrollButton` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 20%: Added Collapse, SvgIcon, Icon, and Link SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: Collapse emits the root/wrapper/wrapperInner transition slot hierarchy with vertical collapsed owner state, SvgIcon emits the default svg root with viewBox/fontSize/color owner state, Icon emits the `material-icons notranslate` span root with fontSize/color state, and Link emits the Typography-derived anchor root with default primary/always/inherit link state. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_collapse`, `sax_svgicon`, `sax_icon`, and `sax_link` exports, prop setters, slot output, and `MuiCollapse`/`MuiSvgIcon`/`MuiIcon`/`MuiLink` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 21%: Added ImageList, ImageListItem, ImageListItemBar, ListItemButton, ListItemAvatar, and ListItemSecondaryAction SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: ImageList emits the standard `ul` root with cols/gap/rowHeight/variant state, ImageListItem emits the standard `li` root with rows/cols state, ImageListItemBar emits root/titleWrap/title/subtitle/actionIcon slots, ListItemButton emits a ButtonBase-like root with gutters/selected/divider/dense state, and the ListItem avatar/secondary action wrappers emit their context-shaped root slots. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains the new `sax_imagelist*` and `sax_listitem*` exports, prop setters, slot output, and `MuiImageList*`/`MuiListItem*` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 22%: Added the Table component family seeds in SA/SAX: TableContainer, Table, TableHead, TableBody, TableFooter, TableRow, TableCell, TableSortLabel, TablePaginationActions, and TablePagination. Defaults follow upstream MUI's React model where current SAX can express it: table section components emit their semantic table roots with default utility classes, TableCell exposes align/padding/size/stickyHeader/variant state, TableSortLabel emits ButtonBase-like label and icon slots, TablePaginationActions emits previous/next IconButton-like slots, and TablePagination emits the TableCell/Toolbar/spacer/select/displayedRows/actions hierarchy. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains the new `sax_table*` exports, prop setters, nested pagination actions, slot output, and `MuiTable*` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 23%: Added CssBaseline, GlobalStyles, ScopedCssBaseline, NoSsr, ClickAwayListener, Unstable_TrapFocus, InitColorSchemeScript, and Grid SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: baseline/global/no-SSR/click-away wrappers expose their owner props and pass children through a root marker, ScopedCssBaseline emits its scoped root, TrapFocus emits a focus-sentinel root hierarchy, InitColorSchemeScript exposes the upstream storage/color-scheme props with a SAX-safe marker, and Grid emits the default root with layout owner state. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains the new wrapper/grid exports, prop setters, slot output, and `MuiCssBaseline`/`MuiGlobalStyles`/`MuiScopedCssBaseline`/`MuiNoSsr`/`MuiClickAwayListener`/`MuiTrapFocus`/`MuiInitColorSchemeScript`/`MuiGrid` marker or utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 24%: Added ToggleButton, ToggleButtonGroup, Snackbar, SnackbarContent, and MobileStepper SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: ToggleButton emits a ButtonBase-like pressed root with color/size/selected state, ToggleButtonGroup emits a horizontal grouped root, Snackbar emits the default bottom-left root with Grow/SnackbarContent hierarchy, SnackbarContent emits Paper/message/action slots, and MobileStepper emits the Paper root with default dots slots. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains the new toggle/snackbar/stepper exports, prop setters, nested SnackbarContent, slot output, and `MuiToggleButton*`/`MuiSnackbar*`/`MuiMobileStepper` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 25%: Added Pagination and PaginationItem SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: Pagination emits the `nav` root and `ul` slot with page item children, while PaginationItem emits a ButtonBase-like root with default text/circular/page classes plus the icon slot used by first/last/previous/next item types. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_pagination*` and `sax_paginationitem*` exports, prop setters, nested PaginationItem calls, and `MuiPagination*`/`MuiPaginationItem*` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 26%: Added SpeedDial, SpeedDialAction, and SpeedDialIcon SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: SpeedDial emits root/Zoom/Fab/actions slots with menu roles and open state, SpeedDialAction emits staticTooltip/staticTooltipLabel/Fab slots, and SpeedDialIcon emits root/openIcon/icon slots with the upstream default add icon path. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_speeddial*`, `sax_speeddialaction*`, and `sax_speeddialicon*` exports, prop setters, nested SpeedDialIcon state propagation, and `MuiSpeedDial*` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 27%: Added Tooltip SA/SAX seed. Defaults follow upstream MUI's React model where current SAX can express it: Tooltip exposes timing, listener, placement, arrow, title, and open owner props, wraps children in a child marker slot, and emits the Popper/Grow/tooltip/arrow slot hierarchy with default bottom placement classes. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_tooltip*` exports, `title`/`open`/placement prop setters, slot output, and `MuiTooltip*` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 28%: Added Rating and Slider SA/SAX seeds. Defaults follow upstream MUI's React model where current SAX can express it: Rating exposes value/defaultValue/max/precision/readOnly/size state and emits root/label/icon/visuallyHidden input slots with the upstream star paths, while Slider exposes range/value/orientation/track/valueLabel state and emits root/rail/track/mark/markLabel/thumb/valueLabel/input slots. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_rating*` and `sax_slider*` exports, value prop setters, slot output, and `MuiRating*`/`MuiSlider*` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 29%: Added Autocomplete SA/SAX seed and reached 130/130 upstream `@mui/material` core component manifest coverage. Defaults follow upstream MUI's React model where current SAX can express it: Autocomplete exposes common owner/default props for popup, clear, loading, filtering, multiple/freeSolo, inputValue/value, size, readOnly, and disabled state, and emits root/inputRoot/input/tag/endAdornment/clearIndicator/popupIndicator/popper/paper/loading/noOptions/listbox/option/groupLabel/groupUl slots with upstream utility classes. Verified the inlined demo with `sa react check` and `sa react build`; generated `app.sa` contains `sax_autocomplete*` exports, prop setters, slot output, and `MuiAutocomplete*` utility class constants. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 30%: Converted `demos/mui_all_components.sax` into a fully inlined, directly verifiable all-components demo and added an explicit TablePaginationActions usage to the basic demo. Both `demos/mui_basic_inlined.sax` and `demos/mui_all_components.sax` now directly instantiate all 130 locked upstream material component exports and define all 130 SA/SAX component seeds in-file, avoiding any Zig source expander or runtime Node/MUI dependency. Verified `sa react check` for both demos, built `demos/mui_all_components.sax`, confirmed generated `app.sa` contains `sax_muiallcomponents*`, `sax_autocomplete*`, `sax_tablepaginationactions*`, and representative `Mui*` utility class constants, and confirmed installed `zig-out/share/demos/*` files match the source demos. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 31%: Switched the primary all-components verification path to SA/SAX library consumption through `sa_plugin_react` generic `--include <file.sax>` composition. Added `demos/mui_all_components_from_library.sax`, which directly instantiates all 130 locked Material component exports while consuming their definitions from `mui/material.sax` instead of inlining them. Verified `sa react check demos/mui_all_components_from_library.sax --include ../mui/material.sax`, built it to `.zig-cache/mui-library-dist`, confirmed generated `app.sa` contains `sax_muiallcomponents*`, `sax_button*`, `sax_autocomplete*`, `sax_tablepaginationactions*`, and representative `Mui*` utility class constants, and added the library-consumption demo to install assets. `zig build test -j1`, component manifest check, and boundary scan remain clean: Zig still only exposes descriptor/API files with `.handle_command = null` and no MUI logic under `src/`.
- 32%: Moved MUI library consumption off manual relative paths by using `sa_plugin_react`'s generic plugin share include resolution. With both React and MUI plugin libs in `SA_PLUGINS_PATH`, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax` resolves `mui/material.sax` from the MUI plugin share assets and compiles the all-components demo. This remains SA/SAX-driven: MUI Zig still only publishes the descriptor and installed share assets, with no source expansion, owner-state logic, class logic, theme logic, or CLI command in `src/`.
- 33%: Added browser/runtime verification for the SA/SAX library-consumption all-components bundle. `tools/verify_mui_browser.mjs` serves a `sa react build` output, opens it in Chromium, waits for `.mui-basic-demo`, and asserts representative Material utility classes including `MuiButton`, `MuiAutocomplete`, `MuiTablePaginationActions`, `MuiDialog`, `MuiSlider`, `MuiRating`, `MuiCard`, `MuiList`, and `MuiTable`, with broad `Mui*` DOM coverage. This also drove generic `sa_plugin_react` fixes for SVG `className` reflection and CSP support for lowered safe style attrs. Verified `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist` after building with `--include mui/material.sax` from MUI plugin share assets; MUI Zig remains descriptor-only.
- 34%: Tightened React children behavior for the MUI SA/SAX component path without moving logic into MUI Zig. `sa_plugin_react` now creates and appends DOM text nodes for initial user-component text children projected through `<Slot />`, so MUI components such as Typography and Button render their child text in the browser instead of leaving empty slot anchors. The MUI browser verifier now asserts projected text (`SA driven MUI`) and representative button labels (`Save`, `Grouped`, `Tooltip target`, `Open`) in addition to broad MUI class coverage. Verified `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist` against the `--include mui/material.sax` bundle.
- 35%: Added the first SA/SAX ownerState utility-class branch in the library path without moving MUI behavior into Zig. Button and ButtonBase now append upstream-aligned global `Mui-disabled` when their `disabled` state is true, using React/SAX static string ternary templates in `mui/material.sax`; both still drive the native `disabled` DOM property through `disabled={disabled}`. The all-components library demo now renders disabled Button/ButtonBase cases, and the Chromium verifier asserts the `Mui-disabled` class plus native disabled property. Verification: component manifest check, `zig build test -j1`, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 36%: Expanded SA/SAX ownerState utility-class branches across more upstream ButtonBase-derived Material components. IconButton, Fab, MenuItem, StepButton, Tab, ToggleButton, PaginationItem, CardActionArea, and ListItemButton now append upstream global state classes such as `Mui-disabled` and `Mui-selected` from their SA/SAX state where the upstream React `useUtilityClasses(ownerState)` or ButtonBase forwarding model applies. The library-consumption all-components demo now includes disabled/selected instances for these components, and the Chromium verifier asserts each state class with text-specific selectors plus native `disabled` for button-backed roots. Verification: component manifest check, MUI `zig build test -j1`, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 37%: Added SwitchBase-derived checked/disabled ownerState utility classes in SA/SAX. Checkbox and Radio roots now append `Mui-checked` and `Mui-disabled` from their `checked`/`disabled` state, and Switch applies the same global classes to its `switchBase` slot, matching the upstream SwitchBase forwarding and Switch slot utility-class model. The all-components library demo now includes checked/disabled Checkbox, Radio, and Switch instances; the Chromium verifier asserts their state classes and validates the underlying input `checked`/`disabled` properties. Verification: component manifest check, MUI `zig build test -j1`, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 38%: Expanded upstream-aligned Form/Input ownerState utility classes in SA/SAX without moving MUI behavior into Zig. FormControl, FormLabel, FormHelperText, InputLabel, InputBase, Input, FilledInput, OutlinedInput, and TextField now project current boolean state into utility classes such as `MuiFormControl-fullWidth`, `Mui-error`, `Mui-focused`, `Mui-required`, `Mui-readOnly`, `MuiInputBase-fullWidth`, `MuiInputBase-hiddenLabel`, `MuiInputBase-multiline`, `MuiFilledInput-hiddenLabel`, `MuiFilledInput-multiline`, and `MuiOutlinedInput-multiline` where the upstream `useUtilityClasses(ownerState)` model applies. The library demo and Chromium verifier now cover these form/input state cases plus native `disabled`/`readOnly` input properties. This also drove a generic `sa_plugin_react` fix so parser-normalized React component props like `readOnly`, `autoFocus`, and `className` map back to camelCase child state setters rather than requiring MUI-specific workarounds. Verification: React `zig test src/react/lowerer.zig`, React `zig build test -j1`, component manifest check, MUI `zig build test -j1`, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 39%: Added upstream-aligned Step/Accordion ownerState utility classes in SA/SAX. Stepper now emits `MuiStepper-alternativeLabel`, Step emits global `Mui-completed`, StepButton forwards active/completed/disabled state to its nested StepLabel label slot, StepConnector emits `Mui-active`, `Mui-completed`, `Mui-disabled`, and `MuiStepConnector-alternativeLabel`, StepIcon emits `Mui-active`, `Mui-completed`, and `Mui-error`, StepLabel emits root/icon/label state classes for error/disabled/active/completed/alternativeLabel, Accordion emits `Mui-expanded` and `Mui-disabled`, and AccordionSummary emits expanded/disabled root plus expanded content classes with native `disabled` on the button root. The all-components library demo and Chromium verifier now assert these state classes in the browser. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 40%: Added upstream-aligned Table ownerState utility classes in SA/SAX. Table now emits `MuiTable-stickyHeader`, TableRow emits `MuiTableRow-hover` and global `Mui-selected`, TableCell emits `MuiTableCell-stickyHeader`, and TableSortLabel emits global `Mui-active`, matching the upstream `useUtilityClasses(ownerState)` model for directly expressible boolean state. The all-components library demo now renders a sticky header table with selected/hover row, sticky cell, and active sort label, and the Chromium verifier asserts all four state classes in the browser. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 41%: Added upstream-aligned List/Chip/Badge ownerState utility classes in SA/SAX without moving behavior into Zig. List now conditionally emits `MuiList-padding` and `MuiList-dense`, ListItem emits dense/gutters/padding/divider classes, ListItemButton emits dense/gutters/divider plus existing selected/disabled state, ListItemText emits inset/dense classes, ListItemSecondaryAction emits `MuiListItemSecondaryAction-disableGutters`, ListSubheader conditionally emits gutters/sticky/inset classes, Chip emits global `Mui-disabled` and `MuiChip-clickable`, and Badge emits `MuiBadge-invisible`. The all-components library demo now renders compact list, disabled clickable chip, and invisible badge cases, and the Chromium verifier asserts both present state classes and removed default classes for disable* props. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 42%: Added upstream-aligned Backdrop/Rating/Slider/CircularProgress ownerState utility classes in SA/SAX. Backdrop now emits `MuiBackdrop-invisible`, Rating emits global `Mui-disabled` and `MuiRating-readOnly`, Slider emits global `Mui-disabled` on root and thumb plus native disabled input state, and CircularProgress emits `MuiCircularProgress-circleDisableShrink` on the circle slot. The all-components library demo now renders invisible backdrop, disabled/read-only rating, disabled slider, and disable-shrink circular progress cases, and the Chromium verifier asserts the state classes plus native disabled input behavior. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 43%: Added upstream-aligned navigation/overlay ownerState utility classes in SA/SAX. ButtonGroup now emits `MuiButtonGroup-fullWidth` and `MuiButtonGroup-disableElevation`, BottomNavigationAction conditionally emits `MuiBottomNavigationAction-iconOnly` plus global `Mui-selected` on root and label, TabScrollButton emits global `Mui-disabled`, SpeedDial removes `MuiSpeedDial-actionsClosed` when open, SpeedDialAction emits/removes `MuiSpeedDialAction-fabClosed` and `MuiSpeedDialAction-staticTooltipClosed` from its `open` state, and Tooltip conditionally emits `MuiTooltip-popperInteractive`, `MuiTooltip-popperArrow`, and `MuiTooltip-tooltipArrow`. The all-components library demo now renders full-width button group, selected/show-label bottom navigation actions, disabled tab scroll button, open and closed speed dial actions, arrow tooltip, and disable-interactive tooltip cases; the Chromium verifier asserts present state classes and removed closed/iconOnly/interactive classes where applicable. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 44%: Added upstream-aligned base surface/action ownerState utility classes in SA/SAX. Toolbar now conditionally emits `MuiToolbar-gutters`, Paper conditionally emits `MuiPaper-rounded` from `square`, Typography emits `MuiTypography-gutterBottom` and `MuiTypography-noWrap`, and AccordionActions/DialogActions/CardActions conditionally emit their default spacing classes from `disableSpacing`. The all-components library demo now renders flush toolbar, square paper, compact no-wrap typography, and tight action rows; the Chromium verifier asserts the typography state classes and confirms the disabled gutters/spacing/rounded classes are removed from the exact target roots. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 45%: Added upstream-aligned layout/dialog ownerState utility classes in SA/SAX. Dialog paper now emits `MuiDialog-paperFullWidth` and `MuiDialog-paperFullScreen`, DialogContent emits `MuiDialogContent-dividers`, Container emits `MuiContainer-fixed` and `MuiContainer-disableGutters`, and Divider emits `MuiDivider-absolute` and `MuiDivider-flexItem`. The all-components library demo now renders fixed/flush container, divided dialog content, full-width/full-screen dialog paper, and absolute flex divider cases; the Chromium verifier asserts those classes in the browser. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 46%: Added upstream-aligned form layout ownerState utility classes in SA/SAX. FormGroup now emits `MuiFormGroup-row` and global `Mui-error`, RadioGroup emits `MuiRadioGroup-row` and global `Mui-error`, and FormControlLabel emits global `Mui-disabled`, `Mui-error`, and `Mui-required` on the root plus global `Mui-disabled` on its label slot. The all-components library demo now renders row/error FormGroup and RadioGroup cases plus disabled/error/required FormControlLabel; the Chromium verifier asserts the root and label-slot state classes. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 47%: Added upstream-aligned InputAdornment/Select/NativeSelect ownerState utility classes in SA/SAX. InputAdornment now emits `MuiInputAdornment-sizeMedium`, `MuiInputAdornment-disablePointerEvents`, and `MuiInputAdornment-hiddenLabel`; Select now forwards disabled/error to its InputBase-compatible root, emits select-slot `Mui-disabled`, `MuiSelect-multiple`, and `Mui-error`, emits icon-slot `MuiSelect-iconOpen`/`Mui-disabled`, and sets the hidden native input disabled property; NativeSelect now emits root disabled/error, select-slot `Mui-disabled`, `MuiNativeSelect-multiple`, and `Mui-error`, icon-slot `Mui-disabled`, and native select disabled/multiple properties. The all-components library demo now renders explicit adornment/select/native-select state cases, and the Chromium verifier asserts the classes and DOM properties. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 48%: Added upstream-aligned Tabs/Tab/ToggleButtonGroup ownerState utility classes in SA/SAX. Tabs now emits `MuiTabs-centered` on the list slot from its `centered` state, Tab now emits `MuiTab-fullWidth` and `MuiTab-wrapped` on its ButtonBase root from direct boolean state alongside existing selected/disabled state, and ToggleButtonGroup now emits `MuiToggleButtonGroup-fullWidth` on its root from direct boolean state. The all-components library demo now renders centered tabs with a full-width wrapped tab and a full-width toggle button group, and the Chromium verifier asserts all three state cases. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 49%: Added upstream-aligned Autocomplete ownerState utility classes in SA/SAX. Autocomplete now emits `MuiAutocomplete-fullWidth` on the root from `fullWidth`, `MuiAutocomplete-popupIndicatorOpen` on the popup indicator from `open`/popup-open state, and `MuiAutocomplete-popperDisablePortal` on the popper slot from `disablePortal`, matching direct boolean branches in upstream `useUtilityClasses(ownerState)`. The all-components library demo now renders an open full-width disable-portal Autocomplete case, and the Chromium verifier asserts all three slot classes in the browser. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 50%: Added upstream-aligned Accordion/AccordionSummary square and gutters ownerState handling in SA/SAX. Accordion now omits `MuiPaper-rounded` and `MuiAccordion-rounded` when `square` is true and omits `MuiAccordion-gutters` when `disableGutters` is true, while AccordionSummary now omits `MuiAccordionSummary-gutters` when `disableGutters` is true. The obsolete `MuiAccordionSummary-contentGutters` output was removed to match the current upstream utility class set. The all-components library demo now renders a square disable-gutters accordion case, and the Chromium verifier asserts the omitted rounded/gutters/contentGutters classes against real DOM output. Verification: component manifest check, MUI `zig build test -j1`, MUI `zig build` share refresh, boundary scan for descriptor-only Zig, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 51%: Added upstream-aligned `ListItemText` multiline ownerState handling in SA/SAX without moving logic into MUI Zig. `ListItemText` now emits `MuiListItemText-multiline` from the same React-style ownerState shape as upstream when both `primary` and `secondary` content are present, and the all-components library demo renders a multiline case that passes `primary` and `secondary` through `sa_plugin_react` string prop setters. Verification: React lowerer truthy-`&&` template support landed generically in `sa_plugin_react`; MUI component manifest check; MUI `zig build test -j1`; MUI `zig build`; React `zig test src/react/lowerer.zig`; React `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`, plus direct DOM inspection confirming `.MuiListItemText-root.MuiListItemText-multiline` for `Multiline list text`.
- 52%: Added upstream-aligned `MobileStepper` ownerState slot/class handling in SA/SAX without moving behavior into MUI Zig. `MobileStepper` root now emits `MuiMobileStepper-positionTop`, `MuiMobileStepper-positionBottom`, and `MuiMobileStepper-positionStatic` from string ownerState comparisons that stay in the generic `sa_plugin_react` lowering layer, while the SA/SAX seed also exposes the upstream slot classes `MuiMobileStepper-dots`, `MuiMobileStepper-dot`, `MuiMobileStepper-dotActive`, and `MuiMobileStepper-progress`. The all-components library demo now renders bottom/top/static/progress cases, and the Chromium verifier asserts the root position classes plus dots/progress slot coverage. Verification: React `zig test src/react/lowerer.zig`; React `zig test src/react/airlock_gen.zig`; React `zig build test -j1`; MUI component manifest check; MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 53%: Moved the default `MobileStepper` position back onto the generic React/SAX ownerState path instead of relying on explicit demo props or hardcoded default classes. `sa_plugin_react` now supports ptr-state initialization from static string literals, and `MobileStepper.position` uses `'bottom' as ptr`, so the default `MuiMobileStepper-positionBottom` class comes from actual state-driven comparison in SA/SAX. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 54%: Moved `InputAdornment` defaults back onto the generic React/SAX ownerState path instead of hardcoded default classes. `InputAdornment.position`, `size`, and `variant` now use static ptr-state defaults (`'start'`, `'medium'`, `'standard'`), and the root class template derives `MuiInputAdornment-positionStart`/`positionEnd`, `MuiInputAdornment-standard`/`filled`/`outlined`, and `MuiInputAdornment-sizeSmall`/`sizeMedium` from state comparisons. The library demo now covers both the default start adornment and an explicit end adornment, and the Chromium verifier asserts both cases. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 55%: Moved a first safe batch of orientation-driven components back onto the generic React/SAX ownerState path instead of hardcoded default classes. `ButtonGroup`, `Stepper`, `StepButton`, `StepConnector`, `StepLabel`, `Tabs`, `TabScrollButton`, and `ToggleButtonGroup` now use static ptr-state defaults with `orientation = 'horizontal' as ptr`, and their utility classes derive `horizontal`/`vertical` variants from state comparisons in SA/SAX, matching the upstream MUI React `default props -> ownerState -> useUtilityClasses` shape where current React lowering can express it. The library demo now covers explicit vertical cases for grouped buttons, stepper, tab scroll buttons, and toggle groups, while the Chromium verifier asserts both default horizontal and explicit vertical utility classes. Current limitation remains explicit: Stepper child orientation propagation is still demo-driven via direct props until generic React hook/context propagation is available in `sa_plugin_react`; no MUI logic was moved into Zig. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 56%: Moved another safe batch of default-prop-driven utility classes back onto the generic React/SAX ownerState path instead of hardcoded default classes. `Button.size` now uses a static ptr-state default with `size = 'medium' as ptr`, so the root derives `MuiButton-sizeSmall`/`MuiButton-sizeMedium`/`MuiButton-sizeLarge` from state comparisons; `Fab` now uses static ptr-state defaults for `color = 'default' as ptr` and `variant = 'circular' as ptr`, so the root derives `MuiFab-circular`/`MuiFab-extended` and `MuiFab-colorDefault`/`MuiFab-primary` from ownerState instead of fixed class strings; and `InputLabel` now uses static ptr-state defaults for `size = 'medium' as ptr` and `variant = 'standard' as ptr`, while `disableAnimation` directly controls presence of `MuiInputLabel-animated`, matching the upstream `default props -> ownerState -> useUtilityClasses` shape where current React lowering can express it. The library demo now covers small/large buttons, primary/extended fabs, and outlined/filled static InputLabel cases, and the Chromium verifier asserts all of those classes plus omission of `MuiInputLabel-animated` when `disableAnimation` is true. Verification: component manifest check; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; direct DOM inspection confirming `MuiButton-sizeSmall`/`MuiButton-sizeLarge` on the rendered buttons and `MuiFab-primary`/`MuiFab-extended` on the rendered fabs; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 57%: Moved a further safe batch of root-slot utility classes onto the generic React/SAX ownerState path without moving behavior into Zig. `Chip.size` now uses a static ptr-state default with `size = 'medium' as ptr`, so both the root and label slots derive `MuiChip-sizeSmall`/`MuiChip-sizeMedium` and `MuiChip-labelSmall`/`MuiChip-labelMedium` from state comparisons; `Toolbar.variant` now uses `variant = 'regular' as ptr`, so the root derives `MuiToolbar-regular`/`MuiToolbar-dense`; and `Link.underline` now uses `underline = 'always' as ptr`, so the root derives `MuiLink-underlineNone`/`MuiLink-underlineHover`/`MuiLink-underlineAlways` from ownerState instead of fixed classes. The library demo now covers a small chip, dense toolbar, and hover/plain link cases, and the Chromium verifier asserts those new utility classes in the browser. Verification: component manifest check; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; direct DOM inspection confirming `MuiChip-sizeSmall` and `MuiChip-labelSmall` on the rendered small chip; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 58%: Moved another safe batch of size-driven utility classes back onto the generic React/SAX ownerState path instead of fixed default class strings. `FormHelperText.size` now uses a static ptr-state default with `size = 'medium' as ptr`, so the root derives `MuiFormHelperText-sizeSmall`/`MuiFormHelperText-sizeMedium` from state comparisons; and `PaginationItem.size` now uses `size = 'medium' as ptr`, so the root derives `MuiPaginationItem-sizeSmall`/`MuiPaginationItem-sizeMedium`/`MuiPaginationItem-sizeLarge` while preserving the existing `shape`, `variant`, and state classes. The library demo now covers a small helper-text case plus explicit small/large pagination items, and the Chromium verifier asserts those utility classes in the browser. Verification: component manifest check; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; direct DOM inspection confirming `MuiPaginationItem-sizeSmall` and `MuiPaginationItem-sizeLarge` on the rendered pagination items; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 59%: Moved a further safe batch of medium-by-default control sizes onto the generic React/SAX ownerState path without moving behavior into Zig. `IconButton.size` now uses a static ptr-state default with `size = 'medium' as ptr`, so the root derives `MuiIconButton-sizeSmall`/`MuiIconButton-sizeMedium`/`MuiIconButton-sizeLarge`; `Checkbox.size` now uses `size = 'medium' as ptr`, so the root derives `MuiCheckbox-sizeSmall`/`MuiCheckbox-sizeMedium`; `Radio.size` now uses `size = 'medium' as ptr`, so the root derives `MuiRadio-sizeSmall` where applicable; and `Switch.size` now uses `size = 'medium' as ptr`, so the root derives `MuiSwitch-sizeSmall`/`MuiSwitch-sizeMedium` from state comparisons instead of fixed classes. The library demo now covers small icon button, small checkbox, small radio, and small switch cases, and the Chromium verifier asserts those size utility classes alongside the existing checked/disabled native-input checks. Verification: component manifest check; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 60%: Moved another safe batch of medium-by-default size ownerState into the generic React/SAX path without moving any MUI behavior into Zig. `Rating.size` now uses `size = 'medium' as ptr`, so the root derives `MuiRating-sizeSmall`/`MuiRating-sizeMedium`/`MuiRating-sizeLarge`; `Slider.size` now uses `size = 'medium' as ptr`, so the root derives `MuiSlider-sizeSmall`/`MuiSlider-sizeMedium` while preserving the existing disabled handling; and `TableCell.size` now uses `size = 'medium' as ptr`, so the body cell root derives `MuiTableCell-sizeSmall`/`MuiTableCell-sizeMedium` from state comparisons instead of fixed default classes. The library demo now covers small/large rating, small slider, and small table-cell cases, and the Chromium verifier asserts those utility classes against rendered DOM. Verification: component manifest check; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 61%: Moved `SvgIcon.fontSize` and `Icon.fontSize` back onto the original MUI-style `default props -> ownerState -> utility classes` path, with the generic prop-name normalization fix kept in `sa_plugin_react` instead of MUI Zig. `SvgIcon.fontSize` now uses `fontSize = 'medium' as ptr`, so the root derives `MuiSvgIcon-fontSizeInherit`/`Small`/`Medium`/`Large`; `Icon.fontSize` now uses the same default-state shape and derives `MuiIcon-fontSizeInherit`/`Small`/`Medium`/`Large`. This batch required a generic `sa_plugin_react` lowerer alias fix for normalized `font-size -> fontSize`, matching React-style camelCase prop handling for user components rather than adding MUI-specific logic. The library demo now covers small/large `SvgIcon` and small/inherit `Icon` cases, and the Chromium verifier asserts those classes in real DOM output. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; component manifest check; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 62%: Moved another safe batch of hardcoded default utility classes back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `ToggleButton.size` now uses `size = 'medium' as ptr`, so the root derives `MuiToggleButton-sizeSmall`/`MuiToggleButton-sizeMedium`/`MuiToggleButton-sizeLarge`; `AppBar.color` and `AppBar.position` now use static ptr-state defaults with `color = 'primary' as ptr` and `position = 'fixed' as ptr`, so the root derives `MuiAppBar-colorDefault`/`Inherit`/`Primary`/`Transparent` and `MuiAppBar-positionFixed`/`Absolute`/`Static`/`Sticky`/`Relative`; and `TableSortLabel.direction` now uses `direction = 'asc' as ptr`, so both the root and icon slots derive `MuiTableSortLabel-directionAsc`/`DirectionDesc` from state comparisons instead of fixed asc classes. The library demo now covers small/large toggle buttons, a static transparent app bar, and a descending table sort label, and the Chromium verifier asserts those classes against real DOM output. This batch also required a generic `sa_plugin_react` ptr-state capacity fix: static string ptr defaults now allocate a small owned-string floor instead of only the literal length, so later React-style prop writes such as `primary -> transparent` remain valid without MUI-specific workarounds. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; React `zig build`; MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 63%: Moved another safe batch of fixed default utility classes back onto the original MUI React `default props -> ownerState -> utility classes` path without moving any behavior into Zig. `Fab.size` now uses `size = 'large' as ptr`, so the root derives `MuiFab-sizeSmall`/`MuiFab-sizeMedium`/`MuiFab-sizeLarge` from state comparisons instead of a fixed large class; and `Pagination.variant` now uses `variant = 'text' as ptr`, so the root derives `MuiPagination-text`/`MuiPagination-outlined` from ownerState instead of a hardcoded text variant class. The library demo now covers small/medium fabs and an outlined pagination case, and the Chromium verifier asserts those classes in real DOM output alongside the existing `Fab` color/variant and `PaginationItem` checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 64%: Moved another safe batch of fixed default utility classes back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Button.variant` and `Button.color` now use static ptr-state defaults with `variant = 'text' as ptr` and `color = 'primary' as ptr`, so the root derives `MuiButton-text`/`Outlined`/`Contained` and `MuiButton-colorPrimary`/`ColorInherit` from state comparisons instead of fixed default classes; `ButtonGroup.variant` and `ButtonGroup.color` now use `variant = 'outlined' as ptr` and `color = 'primary' as ptr`, so the root derives `MuiButtonGroup-text`/`Outlined`/`Contained` and `MuiButtonGroup-colorPrimary`/`ColorInherit`; and `Tab.textColor` now uses `textColor = 'inherit' as ptr`, so the root derives `MuiTab-textColorInherit`/`Primary`/`Secondary` from ownerState instead of a hardcoded inherit class. The library demo now covers contained/inherit button cases, a contained inherit button group, and primary/secondary tab text-color cases, and the Chromium verifier asserts those classes in real DOM output alongside the existing size, orientation, and selected/disabled checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 65%: Moved another safe batch of fixed default utility classes back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Chip.variant` and `Chip.color` now use static ptr-state defaults with `variant = 'filled' as ptr` and `color = 'default' as ptr`, so the root derives `MuiChip-filled`/`Outlined` and `MuiChip-colorDefault`/`ColorPrimary` from state comparisons instead of fixed default classes; and `FormLabel.color` now uses `color = 'primary' as ptr`, so the root derives `MuiFormLabel-colorPrimary`/`ColorSecondary` from ownerState instead of a hardcoded primary class. The library demo now covers outlined/primary chip cases plus a secondary form-label case, and the Chromium verifier asserts those classes in real DOM output alongside the existing chip size/clickable and form-label state checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 66%: Moved another safe batch of fixed default progress utility classes back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `CircularProgress.color` and `CircularProgress.variant` now use static ptr-state defaults with `color = 'primary' as ptr` and `variant = 'indeterminate' as ptr`, so the root derives `MuiCircularProgress-colorPrimary`/`ColorInherit` and `MuiCircularProgress-indeterminate`/`Determinate`, while the circle slot derives `MuiCircularProgress-circleIndeterminate`/`CircleDeterminate`; `LinearProgress.color` and `LinearProgress.variant` now use `color = 'primary' as ptr` and `variant = 'indeterminate' as ptr`, so the root derives `MuiLinearProgress-colorPrimary`/`ColorInherit` and `MuiLinearProgress-indeterminate`/`Determinate`/`Buffer`, while the bar slots derive the corresponding `bar1Determinate`/`bar1Buffer`/`bar2Buffer` utility classes from state comparisons instead of fixed default class strings. The library demo now covers circular determinate/inherit and linear determinate/buffer cases, and the Chromium verifier asserts those classes in real DOM output alongside the existing disable-shrink progress checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 67%: Moved another safe batch of fixed default utility classes back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Skeleton.variant` and `Skeleton.animation` now use static ptr-state defaults with `variant = 'text' as ptr` and `animation = 'pulse' as ptr`, so the root derives `MuiSkeleton-text`/`Rectangular`/`Rounded`/`Circular` and `MuiSkeleton-pulse`/`Wave` from state comparisons instead of fixed default classes; and `FormHelperText.variant` now uses `variant = 'standard' as ptr`, so the root derives `MuiFormHelperText-contained` for the upstream contained ownerState cases (`filled` and `outlined`) instead of a hardcoded default class. The library demo now covers rectangular and wave skeleton cases plus filled and outlined helper-text cases, and the Chromium verifier asserts those classes in real DOM output alongside the existing helper-text size/state checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 68%: Moved another safe batch of fixed default utility classes back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Badge.color` now uses `color = 'default' as ptr`, so the badge slot derives `MuiBadge-colorPrimary` and `MuiBadge-colorSecondary` only from explicit ownerState instead of always emitting a fixed primary class, while preserving the existing `standard`/`dot`, `overlap*`, and `invisible` utility classes. The library demo now covers explicit primary and secondary badge cases, and the Chromium verifier asserts those classes in real DOM output alongside the existing badge overlap and invisible checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 69%: Moved `Snackbar.anchorOrigin` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Snackbar` now uses static ptr-state defaults with `anchorOriginVertical = 'bottom' as ptr` and `anchorOriginHorizontal = 'left' as ptr`, so the root derives the directional utility classes (`MuiSnackbar-anchorOriginTop|Bottom`, `Left|Center|Right`) and combined ownerState utility classes such as `MuiSnackbar-anchorOriginTopRight` and `MuiSnackbar-anchorOriginBottomCenter` from state comparisons instead of a fixed default class string. This batch required a generic `sa_plugin_react` lowering extension for static string template conditions of the form `ptr == 'literal' && ptr == 'literal'`, keeping the capability in the React/SAX layer rather than introducing MUI-specific logic. The library demo now covers default, top-right, and bottom-center snackbar cases, and the Chromium verifier asserts those classes against real DOM output. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; React `zig build`; MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 70%: Moved `PaginationItem.variant` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `PaginationItem` now uses a static ptr-state default with `variant = 'text' as ptr`, so the root derives `MuiPaginationItem-text` and `MuiPaginationItem-outlined` from ownerState comparisons instead of a fixed default `text` class string, while preserving the existing `shape`, `size`, `page`, `selected`, and `disabled` classes. The library demo now covers an explicit outlined pagination item case, and the Chromium verifier asserts both the default text variant and explicit outlined variant against real DOM output. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 71%: Moved `IconButton.color` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `IconButton` now uses a static ptr-state default with `color = 'default' as ptr`, so the root omits any color utility class for the default case and derives `MuiIconButton-colorPrimary` and `MuiIconButton-colorInherit` only from explicit ownerState, matching upstream `color !== 'default' && color${capitalize(color)}` behavior instead of a fixed `MuiIconButton-colorDefault` class string. The library demo now covers explicit primary and inherit icon-button cases, and the Chromium verifier asserts those classes against real DOM output alongside the existing size and disabled checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 72%: Moved `Checkbox.color`, `Radio.color`, and `Switch.color` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. Each control now uses a static ptr-state default with `color = 'primary' as ptr`, so their roots or switch-base slots derive `MuiCheckbox-colorPrimary`/`MuiCheckbox-colorSecondary`, `MuiRadio-colorPrimary`/`MuiRadio-colorSecondary`, and `MuiSwitch-colorPrimary`/`MuiSwitch-colorSecondary` from ownerState comparisons instead of fixed primary class strings. The library demo now covers explicit secondary checkbox, radio, and switch cases, and the Chromium verifier asserts those color utility classes against real DOM output alongside the existing size, checked, and disabled checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 73%: Moved the fixed `MuiInputBase-colorPrimary` chain for `InputBase`, `Input`, `FilledInput`, and `OutlinedInput` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. Each component now uses a static ptr-state default with `color = 'primary' as ptr`, so the root derives `MuiInputBase-colorPrimary` and `MuiInputBase-colorSecondary` from ownerState comparisons instead of a fixed primary class string, while preserving the existing disabled, error, focused, fullWidth, hiddenLabel, multiline, and readOnly classes. The library demo now covers explicit secondary input, base-input, filled-input, and outlined-input cases, and the Chromium verifier asserts those secondary color utility classes against real DOM output alongside the existing state checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 74%: Moved the fixed `MuiInputBase-colorPrimary` chain for `TextField`, `Select`, and `NativeSelect` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. Each component now uses a static ptr-state default with `color = 'primary' as ptr`, so their wrapped input roots derive `MuiInputBase-colorPrimary` and `MuiInputBase-colorSecondary` from ownerState comparisons instead of a fixed primary class string, while preserving the existing disabled, error, fullWidth, multiline, multiple, variant, and icon/select-slot utility classes. The library demo now covers explicit secondary text-field, select, and native-select cases, and the Chromium verifier asserts those secondary input color utility classes against real DOM output alongside the existing select/native-select state checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 75%: Moved `SpeedDial.direction` and `MenuList.disablePadding` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `SpeedDial` now uses a static ptr-state default with `direction = 'up' as ptr`, so the root derives `MuiSpeedDial-directionUp`/`Down`/`Left`/`Right` from ownerState comparisons instead of a fixed up-direction class string; `MenuList` now exposes `disablePadding = false as i1`, so the root derives `MuiList-padding` only when padding is enabled instead of always emitting a fixed padding class. The library demo now covers a left-directed speed dial and a flush menu-list case, and the Chromium verifier asserts the left-direction class plus omission of `MuiList-padding` for the flush menu list against real DOM output. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 76%: Moved `Slider.color` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Slider` now uses a static ptr-state default with `color = 'primary' as ptr`, so the root derives `MuiSlider-colorPrimary` and `MuiSlider-colorSecondary` from ownerState comparisons instead of a fixed primary color class string, while preserving the existing size and disabled classes plus thumb disabled state. The library demo now covers an explicit secondary slider case, and the Chromium verifier asserts that secondary color utility class against real DOM output alongside the existing small and disabled slider checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 77%: Moved `Fab.color` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Fab` continues to use the upstream default `color = 'default' as ptr`, and the root now derives `MuiFab-colorDefault`, `MuiFab-primary`, and `MuiFab-colorInherit` from ownerState comparisons instead of omitting the inherit branch. The library demo now covers an explicit inherit-fab case, and the Chromium verifier asserts that inherit color utility class against real DOM output alongside the existing default, primary, extended, size, and disabled Fab checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 78%: Moved `ToggleButton.color` and `PaginationItem.type` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `ToggleButton` now uses the upstream default `color = 'standard' as ptr`, so the root derives `MuiToggleButton-standard`, `MuiToggleButton-primary`, and `MuiToggleButton-secondary` from ownerState comparisons instead of a fixed default `standard` class string; `PaginationItem` now uses the upstream default `type = 'page' as ptr`, so the root derives `MuiPaginationItem-page`, `MuiPaginationItem-previousNext`, `MuiPaginationItem-ellipsis`, and `MuiPaginationItem-firstLast` from ownerState comparisons instead of a fixed `page` class string. The library demo now covers primary/secondary toggle buttons plus explicit previous/next/ellipsis/last pagination items, and the Chromium verifier asserts those utility classes against real DOM output alongside the existing toggle size/selected/disabled and pagination variant/shape/size checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 79%: Moved `Badge.anchorOrigin` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Badge` now uses static ptr-state defaults with `anchorOriginVertical = 'top' as ptr`, `anchorOriginHorizontal = 'right' as ptr`, and the existing `overlap = 'rectangular' as ptr`, so the badge slot derives directional utility classes such as `MuiBadge-anchorOriginTopLeft` and combined ownerState utility classes such as `MuiBadge-anchorOriginTopLeftRectangular` and `MuiBadge-anchorOriginBottomLeftCircular` from state comparisons instead of a fixed default `MuiBadge-anchorOriginTopRightRectangular` class string. This batch required a generic `sa_plugin_react` lowering extension for chained static string comparisons of the form `ptr == 'literal' && ptr == 'literal' && ptr == 'literal'`, keeping the capability in the React/SAX layer rather than introducing MUI-specific logic. The library demo now covers top-left rectangular and bottom-left circular badge cases, and the Chromium verifier asserts those utility classes against real DOM output alongside the existing badge color, invisible, and overlap checks. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; React `zig build`; MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 80%: Moved `Dialog.scroll` and `Dialog.maxWidth` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Dialog` now uses static ptr-state defaults with `scroll = 'paper' as ptr` and `maxWidth = 'sm' as ptr`, so the container derives `MuiDialog-scrollPaper` and `MuiDialog-scrollBody` from ownerState comparisons, while the paper slot derives `MuiDialog-paperWidthXs`/`Sm`/`Md`/`Lg`/`Xl` plus the existing `MuiDialog-paperFullWidth` and `MuiDialog-paperFullScreen` classes from state comparisons instead of fixed default `MuiDialog-scrollPaper` and `MuiDialog-paperWidthSm` class strings. The implementation also removes the legacy fixed `MuiDialog-paperScrollPaper` class because it is not part of the current upstream MUI React utility-class set. The library demo now covers an explicit `scroll="body" maxWidth="md"` dialog case, and the Chromium verifier asserts both the default `scrollPaper`/`paperWidthSm` classes and the explicit `scrollBody`/`paperWidthMd` classes against real DOM output alongside the existing dialog full-width, full-screen, content-divider, and action-spacing checks. Verification: React `zig build test -j1`; React `zig build`; MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 81%: Moved `Container.maxWidth` plus `Paper.variant` and `Paper.elevation` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Container` now uses the upstream default `maxWidth = 'lg' as ptr`, so the root derives `MuiContainer-maxWidthXs`/`Sm`/`Md`/`Lg`/`Xl` from ownerState comparisons instead of a fixed default `MuiContainer-maxWidthLg` class string. `Paper` now uses the upstream defaults `variant = 'elevation' as ptr` and `elevation = 1`, so the root derives `MuiPaper-outlined` / `MuiPaper-elevation` plus the corresponding `MuiPaper-elevation0` through `MuiPaper-elevation24` classes from ownerState comparisons instead of fixed `MuiPaper-elevation` and `MuiPaper-elevation1` class strings, while preserving the existing `MuiPaper-rounded` omission for `square`. This batch required a generic `sa_plugin_react` lowering extension for mixed static string conditions of the form `ptr == 'literal' && i64 == literal`, keeping the capability in the React/SAX layer rather than introducing MUI-specific logic. The library demo now covers a medium container plus outlined and elevation-3 paper cases, and the Chromium verifier asserts the default `maxWidthLg` and explicit `maxWidthMd`, `MuiPaper-outlined`, and `MuiPaper-elevation3` classes against real DOM output alongside the existing square-paper checks. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; React `zig build`; MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 82%: Moved `MobileStepper.variant` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `MobileStepper` now uses the upstream default `variant = 'dots' as ptr`, so the dots slot derives `MuiMobileStepper-dots` only for the default dots variant and the progress slot derives `MuiMobileStepper-progress` only for `variant='progress'`, instead of treating the default dots shape as a fixed class string. This batch required a generic `sa_plugin_react` lowering extension for static string ternaries on generic string DOM properties, so `className={variant == 'dots' ? ... : ...}` lowers through the shared React/SAX path rather than any MUI-specific workaround. The library demo now covers default bottom/top/static steppers plus an explicit progress stepper, and the Chromium verifier asserts dots-slot presence for the default cases and progress-slot presence for the progress case against real DOM output. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; React `zig build`; MUI `zig build test -j1`; MUI `zig build`; `sa react check /tmp/mobile_stepper_min.sax --include /tmp/mobile_stepper_only_include.sax`; `sa react check /tmp/mobile_stepper_progress_min.sax --include /tmp/mobile_stepper_only_include.sax`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 83%: Moved `Typography.variant` and `Typography.align` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Typography` now uses the upstream defaults `variant = 'body1' as ptr` and `align = 'inherit' as ptr`, so the root derives the full current utility-class set for `h1`/`h2`/`h3`/`h4`/`h5`/`h6`/`subtitle1`/`subtitle2`/`body1`/`body2`/`inherit`/`button`/`caption`/`overline` plus `MuiTypography-alignLeft`/`Right`/`Center`/`Justify`, instead of hardcoding only `MuiTypography-body1`. The library demo now covers an explicit `h6` typography case plus a centered body-text case, and the Chromium verifier asserts those classes alongside the existing `gutterBottom` and `noWrap` checks. Verification: React `zig test src/react/lowerer.zig`; MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 84%: Moved `Card.raised` back onto the original MUI React `default props -> ownerState -> Paper elevation` path without moving behavior into Zig. `Card` still emits the `MuiCard-root`/Paper root shape, but now uses its `raised` ownerState to derive `MuiPaper-elevation8` for the raised case and `MuiPaper-elevation1` otherwise, instead of hardcoding only the non-raised default elevation branch. This keeps the behavior in SA/SAX and follows upstream `elevation={raised ? 8 : undefined}` semantics closely within the current Paper seed. The library demo now covers an explicit raised-card case, and the Chromium verifier asserts `MuiCard-root.MuiPaper-elevation8` for that rendered card. Verification: MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 85%: Moved `Tooltip.placement` and title rendering back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Tooltip` now uses the upstream default `placement = 'bottom' as ptr`, derives `MuiTooltip-tooltipPlacementBottom`/`Top`/`Left`/`Right` from ownerState comparisons, emits `data-popper-placement` from the same state, and renders `title` inside the tooltip slot while preserving the arrow child. This batch required a generic `sa_plugin_react` lowerer extension for mixed text-and-element DOM children, so a SAX node like `{title}<span />` updates an owned text node instead of replacing the parent `textContent` and deleting child elements. The library demo now covers an explicit top-placement tooltip, and the Chromium verifier asserts the top placement utility class, title text, and retained `MuiTooltip-arrow` child against real DOM output. Verification: React `zig test src/react/lowerer.zig`; React `zig build test -j1`; React `zig build`; MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 86%: Moved `Divider.variant`, `Divider.orientation`, and `Divider.textAlign` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Divider` now uses static ptr-state defaults with `variant = 'fullWidth' as ptr`, `orientation = 'horizontal' as ptr`, and `textAlign = 'center' as ptr`, so the root derives `MuiDivider-fullWidth`/`inset`/`middle`, `MuiDivider-vertical`, and horizontal text alignment classes from ownerState comparisons instead of hardcoding `MuiDivider-fullWidth`. The library demo now covers middle, inset, vertical flex-item, and left-aligned divider cases, and the Chromium verifier asserts those classes against real DOM output alongside the existing absolute/flexItem checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 87%: Moved `ListSubheader.color` back onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `ListSubheader` now uses the upstream default `color = 'default' as ptr`, so the root omits color classes for the default case and derives `MuiListSubheader-colorPrimary` and `MuiListSubheader-colorInherit` from ownerState comparisons, while preserving `gutters`, `sticky`, and `inset` behavior. The library demo now covers explicit primary and inherit list subheaders, and the Chromium verifier asserts those utility classes against real DOM output alongside the existing inset and disabled-gutters/sticky checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 88%: Moved `ButtonGroup.color` and the default `size` owner state back toward the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `ButtonGroup` now uses `size = 'medium' as ptr` for the upstream default owner state, and its root derives the standard color utility classes for `inherit`, `primary`, `secondary`, `error`, `info`, `success`, and `warning` from ownerState comparisons instead of only covering primary/inherit. The library demo now covers an explicit secondary button group, and the Chromium verifier asserts `MuiButtonGroup-colorSecondary` against real DOM output alongside the existing variant, orientation, fullWidth, disableElevation, and inherit color checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 89%: Reconciled `Checkbox.color` with the current source of truth after finding the progress record ahead of the SAX implementation. `Checkbox` now uses the upstream default `color = 'primary' as ptr`, so the default root derives `MuiCheckbox-colorPrimary` from ownerState comparisons just like `Radio` and `Switch`, instead of relying on an empty allocated buffer. The Chromium verifier now asserts the default primary utility classes for Checkbox, Radio, and Switch against real DOM output, in addition to the existing secondary, size, checked, and disabled checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 90%: Moved `Chip.color` further onto the original MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Chip` already used the upstream default `color = 'default' as ptr`; its root now derives the standard color utility classes for `default`, `primary`, `secondary`, `error`, `info`, `success`, and `warning` from ownerState comparisons instead of only covering default/primary. The library demo now covers an explicit secondary chip, and the Chromium verifier asserts `MuiChip-colorSecondary` against real DOM output alongside the existing variant, primary color, size, disabled, and clickable checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 91%: Expanded `Button.color` further along the upstream MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Button` keeps the upstream default `color = 'primary' as ptr`; its root now derives the standard color utility classes for `inherit`, `primary`, `secondary`, `success`, `error`, `info`, and `warning` from ownerState comparisons instead of only covering primary/inherit. The library demo now covers an explicit secondary button, and the Chromium verifier asserts `MuiButton-colorSecondary` against real DOM output alongside the existing variant, size, disabled, and inherit-color checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 92%: Expanded `IconButton.color` further along the upstream MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `IconButton` keeps the upstream default `color = 'default' as ptr`, so the default root still omits color utility classes, while explicit owner state now derives the standard non-default color utility classes for `inherit`, `primary`, `secondary`, `error`, `info`, `success`, and `warning` instead of only covering inherit/primary. The library demo now covers an explicit secondary icon button, and the Chromium verifier asserts `MuiIconButton-colorSecondary` against real DOM output alongside the existing primary, inherit, size, and disabled checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 93%: Corrected `Fab.color` to the current upstream MUI React utility-class expression without moving behavior into Zig. `Fab` keeps the upstream default `color = 'default' as ptr`; its root now follows `color === 'inherit' ? 'colorInherit' : color`, so the default case derives `MuiFab-default`, explicit inherit derives `MuiFab-colorInherit`, and standard palette colors derive `MuiFab-primary`, `MuiFab-secondary`, `MuiFab-error`, `MuiFab-info`, `MuiFab-success`, and `MuiFab-warning` instead of the stale `MuiFab-colorDefault` branch and primary/inherit-only coverage. The library demo now covers an explicit secondary Fab, and the Chromium verifier asserts both default and secondary Fab utility classes against real DOM output alongside the existing primary, inherit, variant, size, and disabled checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 94%: Expanded `Badge.color` further along the upstream MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Badge` keeps the upstream default `color = 'default' as ptr`, so default badges still omit color classes, while explicit owner state now derives `MuiBadge-colorPrimary`, `MuiBadge-colorSecondary`, `MuiBadge-colorError`, `MuiBadge-colorInfo`, `MuiBadge-colorSuccess`, and `MuiBadge-colorWarning` instead of only covering primary/secondary. The library demo now covers an explicit error badge, and the Chromium verifier asserts `MuiBadge-colorError` against real DOM output alongside the existing color, invisible, anchor-origin, overlap, and variant checks. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 95%: Re-aligned `CircularProgress.color` and `LinearProgress.color` with the current upstream MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `CircularProgress` keeps the upstream default `color = 'primary' as ptr` and now derives the standard palette plus inherit utility classes from `color${capitalize(color)}`. `LinearProgress` now derives the same root color classes, removes the stale `MuiLinearProgress-barColorPrimary` branch, and applies the color utility class to `bar2` only for the buffer variant, matching upstream `bar2: ['bar', 'bar2', variant === 'buffer' && color...]`. The library demo now covers secondary circular and linear progress cases plus a secondary buffer progress case, and the Chromium verifier asserts those utility classes while also checking that `MuiLinearProgress-barColorPrimary` is absent. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 96%: Expanded `AppBar.color` one step further along the current upstream MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `AppBar` keeps the upstream defaults `color = 'primary' as ptr` and `position = 'fixed' as ptr`; its root now derives `MuiAppBar-colorSecondary` from ownerState comparisons in addition to the existing default/inherit/primary/transparent classes. The library demo now reuses the existing static AppBar case for an explicit secondary color so the full demo stays within the current compile-size boundary, and the Chromium verifier asserts `MuiAppBar-colorSecondary` against real DOM output. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 97%: Moved `PaginationItem.color` back onto the current upstream MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `PaginationItem` now uses the upstream default `color = 'standard' as ptr`, so the default root omits color classes, while explicit owner state derives `MuiPaginationItem-colorPrimary` and `MuiPaginationItem-colorSecondary` from comparisons instead of ignoring color entirely. The library demo now reuses the existing selected page item for an explicit secondary color, and the Chromium verifier asserts `MuiPaginationItem-colorSecondary` against real DOM output. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 98%: Added the missing `ToggleButton.fullWidth` root utility class branch from the upstream MUI React class table without moving behavior into Zig. `ToggleButton` already carried `fullWidth` state; the root now derives `MuiToggleButton-fullWidth` from ownerState alongside size/color/selected/disabled classes. The library demo reuses the existing full-width toggle example by setting `fullWidth` on the child toggle, and the Chromium verifier asserts the resulting class. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 99%: Re-aligned `SvgIcon.color` with the current upstream MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `SvgIcon` now uses the upstream default `color = 'inherit' as ptr`, so the default root omits color classes, while explicit owner state derives the current `MuiSvgIcon-colorPrimary`, `MuiSvgIcon-colorSecondary`, `MuiSvgIcon-colorAction`, `MuiSvgIcon-colorError`, and `MuiSvgIcon-colorDisabled` utility classes. The library demo reuses the existing small icon as an explicit secondary case, and the Chromium verifier asserts `MuiSvgIcon-colorSecondary` against real DOM output. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.
- 100%: Re-aligned `Icon.color` with the current upstream MUI React `default props -> ownerState -> utility classes` path without moving behavior into Zig. `Icon` now uses the upstream default `color = 'inherit' as ptr`, so the default root omits color classes, while explicit owner state derives the current `MuiIcon-colorPrimary`, `MuiIcon-colorSecondary`, `MuiIcon-colorAction`, `MuiIcon-colorError`, and `MuiIcon-colorDisabled` utility classes. The library demo reuses the existing small material icon as an explicit secondary case, and the Chromium verifier asserts `MuiIcon-colorSecondary` against real DOM output. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; and `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`.

## Post-100 Demo and Workflow Checkpoints

- Completed the all-components dashboard delivery requested after 100%: `demos/mui_dashboard.sax` uses the SA React + SA-MUI library path (`--react --include mui/material.sax`) and `assets/mui_dashboard.css` provides the organized responsive dashboard surface. The Vite build path and Chromium verifier cover representative MUI DOM output plus interactive dashboard behavior such as button hover/active state, slider interaction, select focus, menu hover, responsive layout, and overflow checks.
- Completed the Material Kit-inspired demo delivery: `demos/mui_material_kit_demo.sax` and `assets/mui_material_kit_demo.css` build a full SA React + SA-MUI demo using public assets from `/home/vscode/projects/material-kit-react/public` without importing Node MUI, React DOM, Emotion, or `@mui/material`. Playwright verification covered desktop/mobile rendering, loaded images, MUI class coverage, hover behavior, section/card counts, no horizontal overflow, and static asset MIME handling.
- Promoted `sa_plugin_vite` dev mode as the default demo workflow for UI iteration. `sa vite dev ... --react --include mui/material.sax --css ... --public-dir ...` now supports hot reload for SAX sources, React includes, CSS, and public assets, so dashboard and Material Kit demo work can iterate without repeatedly copying static build output. One-shot `sa vite build` remains the release/static verification path.
- Installed the completed demo sources and CSS through the MUI plugin share assets: `demos/mui_dashboard.sax`, `demos/mui_material_kit_demo.sax`, `assets/mui_dashboard.css`, and `assets/mui_material_kit_demo.css` are listed in both `build.zig` and `sap.json`.
- Continued the post-100 infrastructure work in `sa_plugin_react` rather than adding MUI Zig logic: direct text children projected through a user component `<Slot />` now get parent-owned text-node slots and refresh on parent render. This moves SA-MUI wrapper components such as `Button` closer to the original React children/slot behavior for cases like `<Button>Projected {count}</Button>`. Verification covered React lowerer tests, React composition runtime, and a small SA-MUI `Button` render check using `--include mui/material.sax`.
- Tightened the same React/SAX children lifecycle path by storing handles for both static and dynamic projected text nodes and removing projected/native text-node handles during component destroy. This closes a concrete gap left by direct `<Slot />` text projection: wrapper components can now refresh parent-owned text and clean up those handles without any MUI-specific Zig code. Verification covered focused lowerer tests and the full React lowerer test suite.
- Added generic React/SAX slot context prop projection for MUI-style default-prop inheritance without moving behavior into MUI Zig. Components can now declare `<Slot contextProps="..." />`; the React lowerer projects those provider state values into slotted user-component children before explicit child props, so explicit props still win like upstream MUI `resolveProps(contextProps, inProps)`. `ButtonGroup` now propagates `variant`, `color`, `size`, and `disabled` to nested `Button` children, while `List`, `ListItem`, and `ListItemButton` propagate dense/gutter/alignment defaults to their list-family children. Verification covered React lowerer focused/full tests, React `zig build test -j1`, React `zig build`, MUI `zig build test -j1`, MUI `zig build`, `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`, `sa react check demos/mui_material_kit_demo.sax --include mui/material.sax`, `sa react build ... --out-dir .zig-cache/mui-share-dist`, `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`, and `node tools/verify_mui_material_kit_browser.mjs http://127.0.0.1:5174/` against the Vite dev server.
- Extended the same SA/SAX context path to the FormControl family without adding MUI logic to Zig. `FormControl` now carries upstream-style defaults for `margin`, `size`, `variant`, and `filled`, and projects `color`, `disabled`, `error`, `filled`, `focused`, `fullWidth`, `hiddenLabel`, `required`, `size`, and `variant` through `<Slot contextProps="..." />` to form consumers. `InputBase`, `Input`, `FilledInput`, and `OutlinedInput` now derive `MuiInputBase-sizeSmall` from ownerState and forward inherited `required` to the native input. The all-components demo now scopes FormControl around the actual form/input examples so context inheritance does not leak into unrelated components with same-named state, and the Chromium verifier asserts inherited FormLabel/InputLabel/FormHelperText/InputBase classes plus native `input.required`. Verification: MUI `zig build test -j1`; MUI `zig build`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`; `sa react check demos/mui_material_kit_demo.sax --include mui/material.sax`; restarted `sa vite dev` for the Material Kit demo; and `node tools/verify_mui_material_kit_browser.mjs http://127.0.0.1:5174/`.
- Extended the same upstream-style context/default-prop projection to `ToggleButtonGroup` without adding MUI logic to Zig. `ToggleButtonGroup` now projects `color`, `disabled`, `fullWidth`, grouped state, and `size` through `<Slot contextProps="..." />`, while `ToggleButton` consumes that state to emit `MuiToggleButtonGroup-grouped`, inherited color/size/fullWidth classes, and native disabled behavior unless explicit child props override it. The all-components demo now covers inherited fullWidth and disabled secondary small toggles, and the Chromium verifier asserts grouped classes plus native disabled output. Verification: MUI `zig build test -j1`; `sa react check demos/mui_all_components_from_library.sax --include mui/material.sax`; `sa react build ... --out-dir .zig-cache/mui-share-dist`; `node tools/verify_mui_browser.mjs .zig-cache/mui-share-dist`; `sa react check demos/mui_material_kit_demo.sax --include mui/material.sax`; `sa vite dev demos/mui_material_kit_demo.sax --react --include mui/material.sax ... --public-dir /home/vscode/projects/material-kit-react/public`; and `node tools/verify_mui_material_kit_browser.mjs http://127.0.0.1:5174/` against the plugin dev server.

## Remaining Scope Notes

- Not complete yet: React hook/context primitives required by original MUI, full keyed/dynamic children reconciliation beyond the current projected text/element lifecycle coverage, and full dynamic MUI ownerState/default-props/slots/styled/theme semantics. Current SA/SAX seed inventory covers all 130 upstream material component exports in the locked manifest and completed demo/share delivery without moving behavior into Zig.
